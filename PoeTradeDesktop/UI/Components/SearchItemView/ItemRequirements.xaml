<UserControl x:Class="PoeTradeDesktop.UI.Components.SearchItemView.ItemRequirements"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" >
    <Grid>
        <StackPanel x:Name="panel"  HorizontalAlignment="Center">
            <TextBlock x:Name="txtItemLevel" FontSize="1" FontFamily="{StaticResource SmallCaps}" TextAlignment="Center" >
                <Run Text="Item Level: " Foreground="#7F7F7F" FontSize="12"/>
                <Run Text="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=ItemLevel}" Foreground="#FFFFFF" FontSize="12"/>
            </TextBlock>
            <TextBlock x:Name="txtRequirements" TextAlignment="Center" FontSize="1" FontFamily="{StaticResource SmallCaps}" >
                <Run Text="Requires " Foreground="#7F7F7F" FontSize="12"/>
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
